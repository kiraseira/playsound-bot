from the commands directory copy or symlink js files to there to enable the commands.

Try to not just blindly copy in files :) for example command-template.js is a template and not a 
working command.
